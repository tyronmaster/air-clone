import { BookingVariantImagePipe } from './booking-variant-images.pipe';

describe('BookingVariantImagePipe', () => {
  it('create an instance', () => {
    const pipe = new BookingVariantImagePipe();
    expect(pipe).toBeTruthy();
  });
});
