import { RoomsPreviewImagesPipe } from './rooms-preview-images.pipe';

describe('RoomsPreviewImagesPipe', () => {
  it('create an instance', () => {
    const pipe = new RoomsPreviewImagesPipe();
    expect(pipe).toBeTruthy();
  });
});
